<section class="hero" id="top">
  <div class="hero-media" role="img" aria-label="Two silhouetted figures pushing two large jigsaw puzzle pieces together in front of a sunset."></div>
  <div class="hero-scrim"></div>
  <div class="wrap hero-content">
    <h1>The right fit for your business.</h1>
    <p class="lead">
      Mcet Consulting specialises in .NET & Azure based solutions —
      hands-on delivery and technical leadership — for large-scale,
      regulated systems across banking, financial services, and beyond.
    </p>
    <div class="hero-actions">
      <a href="#contact" class="btn">Get in touch</a>
      <a href="#services" class="btn btn-ghost">See the services</a>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 82vh;
    display: flex;
    align-items: center;
    padding: 0;
    overflow: hidden;
  }

  .hero-media {
    position: absolute;
    inset: 0;
    background-image: url('/img/header-bg.jpg');
    background-size: cover;
    background-position: center 60%;
  }

  .hero-scrim {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      100deg,
      rgba(20, 24, 31, 0.92) 0%,
      rgba(20, 24, 31, 0.78) 32%,
      rgba(20, 24, 31, 0.42) 58%,
      rgba(20, 24, 31, 0.55) 100%
    );
  }

  .hero-content {
    position: relative;
    z-index: 1;
    padding: 120px 32px;
  }

  .hero h1 {
    color: #fff;
    font-size: clamp(34px, 5.4vw, 54px);
    max-width: 17ch;
  }

  .lead {
    margin-top: 24px;
    font-size: 19px;
    color: rgba(255, 255, 255, 0.86);
    max-width: 42ch;
  }

  .hero-actions {
    margin-top: 34px;
    display: flex;
    gap: 14px;
    flex-wrap: wrap;
  }

  @media (max-width: 640px) {
    .hero {
      min-height: 70vh;
    }
    .hero-content {
      padding: 80px 20px;
    }
  }
</style>
